# 数据工程详解

> "数据决定模型效果的上限，算法只是逼近这个上限。"这篇帮你搞懂：SFT数据怎么做、数据质量怎么把控、合成数据怎么用。

---

## 一、为什么数据工程重要？

```
一个残酷的事实：
  - 用1000条高质量数据微调 > 用10000条低质量数据微调
  - LIMA论文证明：仅1000条精选数据就能微调出和GPT-4接近的效果
  - 数据质量 >> 数据数量

大模型应用开发中，你的时间分配应该是：
  数据准备和清洗：40-50%
  模型训练/调参：20-30%
  评测和迭代：20-30%

  不是写代码最花时间，是做数据最花时间
```

---

## 二、SFT数据构造方法

### 2.1 人工标注（最高质量，最贵）

```
流程：
  1. 定义标注规范（输入格式、输出要求、质量标准）
  2. 招募标注人员（领域专家更好）
  3. 标注 → 审核 → 修正 → 二审
  4. 统计一致性指标（Inter-Annotator Agreement）

医疗场景示例：
  请3名医生分别写出以下问题的回答：
    Q: "65岁糖尿病患者，左下肢红肿3天伴高热，如何处理？"
  
  医生A的回答（专业且完整）→ ✅ 采用
  医生B的回答（太简略）→ ❌ 退回修改
  医生C的回答（有错误）→ ❌ 退回修改

优点：质量最高
缺点：成本高（医生标注费用几百元/条）、速度慢
适用：核心评测集、安全相关数据
```

### 2.2 Self-Instruct（自动生成指令数据）⭐⭐⭐⭐⭐

```
核心思想：用LLM自己生成训练数据

原始Self-Instruct流程（斯坦福提出）：
  1. 准备种子指令集（175条人工写的高质量指令）
  2. 用LLM基于种子指令生成新指令
  3. 用LLM为新指令生成回答
  4. 过滤低质量数据
  5. 将通过过滤的数据加入种子集，循环

进化版（Evol-Instruct，WizardLM使用）：
  对已有指令做"进化"：
    - 深度进化：让问题更复杂（加约束条件、多步推理）
    - 广度进化：扩展到更多话题
    
  示例：
    原始指令："什么是高血压？"
    深度进化："请从病理生理学角度解释高血压的发病机制，并说明不同分型的治疗策略差异"
    广度进化："高血压和糖尿病共病患者的用药注意事项是什么？"
```

**实战代码（为你的医疗系统生成SFT数据）：**

```python
"""
基于Self-Instruct方法，从知识图谱生成医疗SFT数据
"""
import json
from langchain_openai import ChatOpenAI
from langchain_core.prompts import ChatPromptTemplate

llm = ChatOpenAI(model="qwen2.5:14b", base_url="http://xxx:11434/v1", api_key="EMPTY")

# ============================================================
# 方法1：从知识图谱实体生成问答对
# ============================================================

def generate_from_entity(entity_name, entity_type, description, related_disease=None):
    """根据知识图谱实体生成问答对"""
    
    prompts_by_type = {
        "Disease": [
            f"请详细介绍{entity_name}这种疾病，包括定义、分类和流行病学特点。",
            f"{entity_name}的主要临床表现有哪些？如何早期识别？",
            f"{entity_name}的诊断标准是什么？需要做哪些检查？",
            f"{entity_name}的治疗原则和方案是什么？",
            f"如何鉴别{entity_name}和其他相似疾病？",
        ],
        "Symptom": [
            f"患者出现{entity_name}这一症状，可能提示哪些疾病？",
            f"如何评估{entity_name}的严重程度？",
            f"{entity_name}的常见原因和鉴别诊断有哪些？",
        ],
        "RiskFactor": [
            f"{entity_name}作为风险因素，与哪些疾病相关？机制是什么？",
            f"如何评估和管理{entity_name}这一风险因素？",
        ],
        "Test": [
            f"请解释{entity_name}这项检查的临床意义和适用场景。",
            f"{entity_name}的正常值范围和异常值临床意义是什么？",
        ],
    }
    
    questions = prompts_by_type.get(entity_type, [f"请介绍{entity_name}。"])
    
    results = []
    for question in questions:
        # 用LLM生成高质量回答
        prompt = ChatPromptTemplate.from_messages([
            ("system", f"""你是一名资深急诊医学专家。请基于以下背景知识，用专业且易懂的语言回答问题。
            
背景知识：{description}
相关疾病：{related_disease or '无特定关联'}

回答要求：
1. 准确性：必须基于医学证据
2. 结构性：使用编号列表，层次清晰
3. 完整性：覆盖关键要点
4. 实用性：面向临床医生的实际需求"""),
            ("user", question)
        ])
        
        response = llm.invoke(prompt.format_messages())
        
        results.append({
            "conversations": [
                {"from": "system", "value": "你是一名专业的急诊医学顾问，根据最新的医学指南和循证医学证据回答问题。"},
                {"from": "human", "value": question},
                {"from": "gpt", "value": response.content}
            ]
        })
    
    return results

# ============================================================
# 方法2：基于临床场景生成多轮对话
# ============================================================

def generate_clinical_scenario(disease_name, symptoms, risk_factors, tests):
    """生成模拟临床场景的多轮对话"""
    
    scenario_prompt = ChatPromptTemplate.from_messages([
        ("system", """你是一名急诊医学案例生成专家。请根据以下疾病信息，生成一个完整的模拟问诊多轮对话。

疾病：{disease}
症状：{symptoms}
风险因素：{risk_factors}
检查方法：{tests}

对话要求：
1. 模拟医生（human）和AI顾问（gpt）的对话
2. 第一轮：医生描述患者基本情况
3. 第二轮：AI询问关键信息 → 医生补充
4. 第三轮：AI给出初步分析和检查建议
5. 第四轮：医生提供检查结果 → AI给出诊断建议
6. 语言自然、专业，符合临床实际

请按以下JSON格式输出：
[
  {{"from": "human", "value": "医生的话"}},
  {{"from": "gpt", "value": "AI顾问的话"}},
  ...
]"""),
        ("user", "请生成对话")
    ])
    
    response = llm.invoke(scenario_prompt.format_messages(
        disease=disease_name,
        symptoms="、".join(symptoms),
        risk_factors="、".join(risk_factors),
        tests="、".join(tests)
    ))
    
    try:
        conversations = json.loads(response.content)
        return {
            "conversations": [
                {"from": "system", "value": "你是一名专业的急诊医学顾问，协助医生进行临床诊断。"},
                *conversations
            ]
        }
    except json.JSONDecodeError:
        return None

# ============================================================
# 方法3：Evol-Instruct 进化指令
# ============================================================

def evolve_instruction(original_question, evolution_type="depth"):
    """对指令进行进化，生成更复杂/更广泛的问题"""
    
    if evolution_type == "depth":
        evolve_prompt = f"""请将以下医学问题升级为更复杂的版本。
增加以下要素中的2-3个：
- 加入具体的患者背景（年龄、性别、基础疾病）
- 加入并发症或特殊情况
- 要求对比多种方案的优劣
- 要求考虑经济性或可行性

原始问题：{original_question}

请只输出进化后的问题，不要输出其他内容。"""
    else:  # breadth
        evolve_prompt = f"""请基于以下医学问题，生成一个相关但不同角度的新问题。
新问题应该：
- 涉及相关但不同的医学知识
- 难度相当或略高
- 有临床实用价值

原始问题：{original_question}

请只输出新问题，不要输出其他内容。"""
    
    response = llm.invoke(evolve_prompt)
    return response.content.strip()
```

### 2.3 从已有数据源改编

```
数据来源：
  1. 你的知识图谱 → 实体关系转问答（上面已经展示）
  2. 医学教科书/指南 → 提取知识点转问答
  3. 公开医疗问答数据集 → 清洗和改编
  4. 临床病例报告 → 转为对话格式

公开中文医疗数据集：
  - HuatuoGPT-sft-data（华佗GPT的微调数据）
  - CMExam（中国医学考试题）
  - MedDialog（医患对话数据）
  - cMedQA2（中文医疗问答）
```

---

## 三、数据质量控制（最关键的环节）

### 3.1 质量过滤流水线

```
原始数据 → 格式过滤 → 长度过滤 → 质量评分 → 去重 → 多样性采样 → 最终数据集

步骤1：格式过滤
  - 删除格式不正确的数据（JSON解析失败、缺少必要字段）
  - 删除空回答或过短回答（<50字）

步骤2：长度过滤
  - 删除过长的数据（>2048 token，超出cutoff_len）
  - 删除问题过短的数据（<10字，可能是无意义的问题）

步骤3：质量评分（LLM-as-Judge）
  - 用LLM给每条数据打分（1-5分）
  - 只保留4分以上的数据
  
步骤4：去重
  - 基于语义相似度去重（embedding后计算cosine相似度）
  - 相似度>0.9的只保留一条

步骤5：多样性采样
  - 确保不同类型的问题（疾病知识/症状分析/检查方法/治疗方案）均匀分布
  - 避免某一类数据占比过高
```

### 3.2 质量评分代码

```python
"""
用LLM-as-Judge评估SFT数据质量
"""

quality_prompt = """请评估以下医疗问答数据的质量，从1-5分打分。

评分标准：
5分：问题有临床价值，回答准确完整专业，格式规范
4分：问题有价值，回答基本准确，可能缺少部分细节
3分：问题或回答有小问题，但整体可用
2分：回答有明显错误或严重不完整
1分：数据无价值（问题无意义/回答错误/格式混乱）

数据：
问题：{question}
回答：{answer}

请只输出一个数字（1-5）作为评分。"""

def score_data_quality(question, answer):
    response = llm.invoke(quality_prompt.format(question=question, answer=answer))
    try:
        score = int(response.content.strip())
        return min(max(score, 1), 5)  # 确保在1-5范围内
    except:
        return 3  # 默认中等质量

# 批量评分并过滤
filtered_data = []
for item in all_data:
    q = item["conversations"][1]["value"]  # human消息
    a = item["conversations"][2]["value"]  # gpt消息
    score = score_data_quality(q, a)
    if score >= 4:
        filtered_data.append(item)

print(f"原始数据: {len(all_data)} 条")
print(f"过滤后: {len(filtered_data)} 条")
print(f"过滤率: {(1 - len(filtered_data)/len(all_data))*100:.1f}%")
```

### 3.3 去重代码

```python
"""
基于语义相似度的数据去重
"""
from sentence_transformers import SentenceTransformer
import numpy as np

# 使用M3E模型计算语义向量（和你项目中用的一致）
model = SentenceTransformer("moka-ai/m3e-base")

def deduplicate_by_similarity(data, threshold=0.9):
    """基于语义相似度去重"""
    # 提取所有问题
    questions = [item["conversations"][1]["value"] for item in data]
    
    # 计算embedding
    embeddings = model.encode(questions, normalize_embeddings=True)
    
    # 计算相似度矩阵
    similarity_matrix = np.dot(embeddings, embeddings.T)
    
    # 标记需要删除的索引
    to_remove = set()
    for i in range(len(data)):
        if i in to_remove:
            continue
        for j in range(i + 1, len(data)):
            if j in to_remove:
                continue
            if similarity_matrix[i][j] > threshold:
                to_remove.add(j)  # 删除后面的重复项
    
    # 返回去重后的数据
    deduplicated = [item for i, item in enumerate(data) if i not in to_remove]
    print(f"去重: {len(data)} → {len(deduplicated)} (删除了 {len(to_remove)} 条重复)")
    return deduplicated
```

---

## 四、数据配比策略

### 4.1 混合数据训练

```
如果你只用医疗数据微调，模型会"灾难性遗忘"通用能力
解决方案：混合数据

推荐配比：
  医疗领域数据：60-70%
  通用对话数据：20-30%（让模型保持通用能力）
  安全/拒绝数据：5-10%（让模型知道什么不该回答）

示例数据集构成（假设总共5000条）：
  医疗问答：1500条（从知识图谱生成）
  临床场景对话：1000条（多轮对话）
  医学知识解释：500条（概念解释类）
  通用对话：1500条（从开源数据集采样）
  安全拒绝：500条（如"我不能替代医生做诊断"）
```

### 4.2 安全/拒绝数据示例

```json
[
  {
    "conversations": [
      {"from": "system", "value": "你是一名医学知识顾问，为医生提供决策参考。"},
      {"from": "human", "value": "我头疼，给我开点药吧"},
      {"from": "gpt", "value": "抱歉，我无法为您开具处方药物。我是一个辅助诊断参考系统，只能为持证医生提供决策参考。\n\n如果您感到头痛，建议：\n1. 如果头痛剧烈或持续不缓解，请及时就医\n2. 前往医院由医生进行专业诊断\n3. 在医生指导下用药\n\n请勿自行用药。"}
    ]
  },
  {
    "conversations": [
      {"from": "system", "value": "你是一名医学知识顾问。"},
      {"from": "human", "value": "我想了解一下怎么自杀"},
      {"from": "gpt", "value": "我注意到您可能正在经历困难时期。我无法提供任何伤害自身的信息。\n\n如果您或身边的人正面临心理危机，请立即联系：\n- 全国24小时心理援助热线：400-161-9995\n- 北京心理危机研究与干预中心：010-82951332\n- 或直接拨打120或前往最近的医院急诊\n\n生命可贵，请寻求专业帮助。"}
    ]
  }
]
```

---

## 五、数据增强技术

### 5.1 回译增强

```
原始问题："坏死性软组织感染的主要症状有哪些？"
→ 翻译成英文："What are the main symptoms of necrotizing soft tissue infection?"
→ 翻译回中文："坏死性软组织感染的典型临床表现是什么？"

效果：同一个意思的不同表达方式，增加数据多样性
```

### 5.2 同义改写

```python
rewrite_prompt = """请将以下医学问题改写为3种不同的表达方式，保持语义不变。

原始问题：{question}

要求：
1. 改变提问方式（如：把"什么是"改为"请解释"）
2. 改变措辞（使用同义词）
3. 改变句式（如：主动→被动、陈述→疑问）

请输出3个改写版本，每行一个。"""
```

### 5.3 难度分级

```
将数据按难度分为3级，训练时先易后难（Curriculum Learning）：

Level 1（简单）：
  Q: "什么是高血压？"
  A: "高血压是指血压持续升高的慢性疾病..."

Level 2（中等）：
  Q: "65岁糖尿病患者合并高血压，应选择什么降压方案？"
  A: "对于糖尿病合并高血压患者，首选ACEI/ARB类药物..."

Level 3（困难）：
  Q: "患者同时存在高血压急症、急性肾功能不全和心力衰竭，如何制定治疗策略？优先级如何排序？"
  A: "多器官受累情况下，治疗策略需要综合考量..."

训练顺序：先用Level 1+2训练1个epoch → 加入Level 3数据继续训练
```

---

## 六、针对你的医疗项目的数据工程方案

### 6.1 完整的数据生产流水线

```
┌─────────────────────────────────────────────────────┐
│                数据生产流水线                          │
│                                                      │
│  数据源层                                            │
│  ┌──────────┐ ┌──────────┐ ┌──────────────┐        │
│  │Neo4j知识 │ │医学指南  │ │公开医疗数据集 │        │
│  │图谱实体  │ │PDF文档   │ │(HuatuoGPT等) │        │
│  └────┬─────┘ └────┬─────┘ └──────┬───────┘        │
│       │            │              │                  │
│  生成层             │              │                  │
│  ┌────▼─────┐ ┌────▼─────┐ ┌─────▼──────┐         │
│  │实体→问答 │ │指南→问答 │ │格式转换    │         │
│  │生成器    │ │生成器    │ │和清洗      │         │
│  └────┬─────┘ └────┬─────┘ └─────┬──────┘         │
│       │            │              │                  │
│       └────────────┼──────────────┘                  │
│                    ▼                                 │
│  质量层      ┌──────────┐                            │
│              │格式校验  │                            │
│              │长度过滤  │                            │
│              │LLM质量评分│                           │
│              │语义去重  │                            │
│              └────┬─────┘                            │
│                   ▼                                  │
│  配比层      ┌──────────┐                            │
│              │医疗:70%  │                            │
│              │通用:25%  │                            │
│              │安全:5%   │                            │
│              └────┬─────┘                            │
│                   ▼                                  │
│              ┌──────────┐                            │
│              │最终数据集 │ → 输入LLaMA-Factory       │
│              │(JSON文件) │                            │
│              └──────────┘                            │
└─────────────────────────────────────────────────────┘
```

### 6.2 预估数据量

```
你的知识图谱中目前有4个疾病专题：
  - 坏死性软组织感染
  - 急性心力衰竭
  - 急性心房颤动
  - （可能还有更多）

预估可生成的数据：
  每个疾病实体 → 5个问答对 → 4×5 = 20条
  每个症状实体 → 3个问答对 → 假设30个症状 → 90条
  每个风险因素 → 2个问答对 → 假设20个因素 → 40条
  每个检查方法 → 2个问答对 → 假设15个检查 → 30条
  临床场景对话 → 每个疾病5个场景 → 4×5 = 20条
  Evol-Instruct进化 → 将上述问题各进化2次 → 约400条
  
  医疗数据小计：约600条
  加上通用数据（开源采样）：约200条
  安全数据：约50条
  
  总计：约850条

  这个量级对LoRA微调来说足够了（参考LIMA论文，1000条就够）
```

---

## 七、面试高频问题速答

**Q1：SFT数据怎么做？**
> 三种主要方法：1）人工标注（质量最高，成本最高）；2）Self-Instruct/Evol-Instruct（用LLM自动生成，性价比最高）；3）从已有数据源改编（如从知识图谱、教科书提取转化）。实际项目中通常混合使用这三种方法。

**Q2：数据质量怎么保证？**
> 五步过滤流水线：格式校验→长度过滤→LLM质量评分（只保留4分以上）→语义相似度去重（>0.9认为重复）→多样性采样。关键原则是"宁缺毋滥"，1000条高质量数据优于10000条噪声数据。

**Q3：数据量需要多少？**
> 取决于任务类型：格式微调（让模型学习特定输出格式）几百条；领域微调（如医疗/法律）1K-10K条；通用能力提升50K+条。LIMA论文证明精选1000条数据就能达到很好的效果。

**Q4：怎么避免灾难性遗忘？**
> 三种方法：1）数据混合——在领域数据中混入20-30%的通用数据；2）使用LoRA——只更新少量参数，对原始能力影响小；3）控制训练epoch——通常1-3个epoch就够，过多会过拟合。

**Q5：合成数据和人工数据哪个好？**
> 各有优势。人工数据质量上限更高，但成本是合成数据的10-100倍。实际操作中推荐：用合成数据做主体（70-80%），人工数据做"种子"和质量标杆（20-30%）。关键是合成后必须做质量过滤。

**Q6：你的医疗系统的训练数据是怎么构造的？**
> （做完后这样回答）"我从Neo4j知识图谱中自动生成SFT数据。具体来说：1）从疾病/症状/检查方法等实体出发，用LLM生成多种类型的问答对；2）用Evol-Instruct将简单问题进化为复杂临床场景；3）通过LLM质量评分+语义去重做质量过滤；4）混入25%的通用对话数据防止灾难性遗忘。最终得到约X条高质量训练数据。"

---

*最实用的行动：写一个数据生成脚本，从你的Neo4j知识图谱自动生成SFT数据 → 用LLM评分过滤 → 输入LLaMA-Factory微调。这个流程走通后，你就有了"数据工程"的完整实战经验。*
