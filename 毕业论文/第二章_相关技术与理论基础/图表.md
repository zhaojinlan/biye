# 第二章 相关技术与理论基础 — 图表

> 所有图均与本系统直接相关，无通用技术示意图。

---

## 图 2-1 本系统 LLM 部署与调用架构

```mermaid
graph LR
    subgraph AutoDL["AutoDL GPU服务器"]
        Ollama[Ollama 服务<br/>Qwen2.5-14B]
        Cpolar[cpolar 内网穿透<br/>固定域名:zjlchat.vip.cpolar.cn]
        Ollama -->|端口6006| Cpolar
    end

    subgraph 客户端["客户端（本地）"]
        Agent[LangGraph 多智能体引擎]
    end

    subgraph 服务端["服务端（云端）"]
        KGBuild[知识图谱构建引擎<br/>LLM实体关系抽取]
    end

    Agent -->|"OpenAI兼容API<br/>https://zjlchat.vip.cpolar.cn"| Cpolar
    KGBuild -->|"OpenAI兼容API"| Cpolar
```

---

## 图 2-2 本系统提示工程应用示意

```mermaid
graph TD
    subgraph 系统提示词设计
        P1["角色设定<br/>「你是一名急诊分诊护士，具备五级分诊能力」"]
        P2["任务指令<br/>「请从以下文本中抽取医学实体和关系」"]
        P3["输出格式约束<br/>「以JSON格式返回，包含entities和relations字段」"]
    end

    P1 --> LLM[Qwen2.5-14B<br/>大语言模型]
    P2 --> LLM
    P3 --> LLM

    LLM --> O1["分诊报告<br/>（自然语言）"]
    LLM --> O2["结构化JSON<br/>（实体关系数据）"]
    LLM --> O3["路由决策<br/>（triage/recommend/experts/query）"]
```

---

## 图 2-3 本系统多智能体主流程图

```mermaid
graph TD
    START((开始)) --> supervisor[监督节点<br/>supervisor_node<br/>任务分类与路由]
    supervisor -->|症状描述| triage[分诊节点<br/>triage_node]
    supervisor -->|病史/个人情况| recommend[风险评估节点<br/>recommend_node]
    supervisor -->|检查结果| agen[多专家会诊节点<br/>agen_node]
    supervisor -->|知识查询| other[知识查询节点<br/>other_node]
    supervisor -->|无需处理| END1((结束))
    triage --> END2((结束))
    recommend --> END3((结束))
    agen --> END4((结束))
    other --> END5((结束))
```

---

## 图 2-4 多专家会诊子流程图（agen_node 内部）

```mermaid
graph TD
    START((开始)) --> diag[诊断专家<br/>diagnostic_expert]
    diag --> img[影像专家<br/>imaging_expert]
    diag --> treat[治疗专家<br/>treatment_expert]
    img --> summary[综合总结<br/>summary_node]
    treat --> summary
    summary --> END((结束))
```

---

## 图 2-5 知识图谱示例（医学领域）

```mermaid
graph LR
    D1((坏死性<br/>软组织感染)) -->|HAS_SYMPTOM| S1((剧烈疼痛))
    D1 -->|HAS_SYMPTOM| S2((皮肤红肿))
    D1 -->|HAS_SYMPTOM| S3((发热))
    D1 -->|TREATED_BY| T1((手术清创))
    D1 -->|TREATED_BY| T2((广谱抗生素))
    D1 -->|DIAGNOSED_BY| DT1((LRINEC评分))
    D1 -->|HAS_RISK_FACTOR| R1((糖尿病))
    D1 -->|CAUSED_BY| P1((A组链球菌))
```

---

## 图 2-6 本系统 Neo4j 知识图谱数据模型

```mermaid
graph TD
    Disease((Disease<br/>疾病))
    Symptom((Symptom<br/>症状))
    RiskFactor((RiskFactor<br/>风险因子))
    Pathogen((Pathogen<br/>病原体))
    Treatment((Treatment<br/>治疗方法))
    DiagnosticTest((DiagnosticTest<br/>诊断检查))

    Disease -->|HAS_SYMPTOM| Symptom
    Disease -->|TREATED_BY| Treatment
    Disease -->|DIAGNOSED_BY| DiagnosticTest
    Disease -->|HAS_RISK_FACTOR| RiskFactor
    Disease -->|CAUSED_BY| Pathogen
```

---

## 图 2-7 RAG 工作流程图

```mermaid
graph LR
    Q[用户提问] --> E[文本向量化<br/>M3E-Base]
    E --> R[知识库检索<br/>Top-K相似内容]
    R --> A[构造增强Prompt<br/>问题 + 检索结果]
    A --> G[LLM生成回答<br/>Qwen2.5-14B]
    G --> O[输出回答]
```

---

## 图 2-8 本系统混合检索策略

```mermaid
graph TD
    Q[用户输入] --> S{监督节点<br/>任务分类}
    S -->|风险评估| KG[Neo4j知识图谱<br/>精确查询]
    S -->|多专家会诊| VEC[Redis向量检索<br/>语义匹配]
    S -->|知识查询| BOTH[知识图谱 + 向量检索<br/>双通道融合]
    KG --> LLM[LLM生成回答]
    VEC --> LLM
    BOTH --> LLM
```

---

## 图 2-9 MCP 在本系统中的应用

```mermaid
graph LR
    subgraph 客户端
        Agent[LangGraph<br/>多智能体]
        MC[MCP Client<br/>langchain-mcp-adapters]
    end
    subgraph 服务端
        MS[MCP Server<br/>FastMCP]
        T1[symptom_search_analyze<br/>症状搜索分析]
        T2[get_common_diagnostic_methods<br/>通用诊断检查]
        T3[get_diagnostic_tests_for_disease<br/>疾病特异性检查]
        T4[retrieve_medical_knowledge_vector<br/>向量检索医学知识]
    end
    subgraph 数据层
        Neo4j[(Neo4j<br/>知识图谱)]
        Redis[(Redis<br/>向量索引)]
    end
    Agent --> MC
    MC -->|SSE| MS
    MS --> T1 & T2 & T3 & T4
    T1 & T2 & T3 --> Neo4j
    T4 --> Redis
```
