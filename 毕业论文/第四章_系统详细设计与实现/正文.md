# 第四章 系统详细设计与实现

> 本章是论文核心，多展示实际做了什么（代码片段+截图+运行效果）。代码片段每处≤15行，建议以截图形式放入（不算查重）。

## 4.1 知识图谱自动化构建实现

### 4.1.1 PDF文献解析（Docling）

> **【第1段：Docling介绍】** IBM开源工具，PDF→结构化HTML，保留标题/段落/表格结构。

> **【第2段：关键代码展示】** 展示DocumentConverter的调用代码（从Construct/knowledge_workflow.py截取≤15行）。说明输出的HTML结构。

### 4.1.2 文本清洗与分块

> **【第1段：清洗策略】** 去DOI之前内容、去参考文献之后内容、删多余空格。HTML→Markdown转换。

> **【第2段：分块参数+原因】** RecursiveCharacterTextSplitter, chunk_size=2000, overlap=200。解释递归分割优先级（段落→句子→字符）和重叠区域保持上下文连贯的设计考量。

### 4.1.3 基于LLM的实体关系抽取

> **【第1段：实体关系定义】** 6种实体类型 + 5种关系类型，Pydantic模型约束输出格式。

> **【第2段：Prompt设计+JSON示例】** 展示给LLM的Prompt关键部分（截取代码）。展示一个抽取结果的JSON示例（从_entities.json中截取）。强调零样本、无需标注数据。

### 4.1.4 Neo4j图谱存储

> **【1段】** py2neo操作Neo4j。MERGE而非CREATE避免重复。展示节点/关系创建的关键代码片段。

### 4.1.5 症状向量化索引

> **【1段】** M3E-Base生成768维向量 → Neo4j创建enhanced_symptom_vectors向量索引（HNSW算法）。查询时：用户症状→向量化→余弦相似度检索→匹配症状节点→关联疾病。展示symptom_vectorizer.py关键代码。

## 4.2 多智能体工作流实现

### 4.2.1 LangGraph状态图构建

> **【第1段：State定义】** TypedDict包含messages/has_triaged/has_diagnosis等字段。

> **【第2段：图构建流程】** add_node(5个) → add_edge(固定边) → add_conditional_edges(条件路由) → compile()。InMemorySaver作Checkpointer实现多轮对话记忆。展示Agent/flow.py中构建代码。

### 4.2.2 监督节点——任务路由

> **【第1段：Prompt设计】** 如何让LLM区分"症状描述/病史回答/检查结果/知识查询"四类输入。

> **【第2段：上下文感知路由】** has_triaged/has_diagnosis状态判断逻辑，避免重复执行。展示路由函数代码。

### 4.2.3 分诊节点——并行分诊

> **【第1段：双智能体角色】** 智能体1=医学顾问（MCP工具），智能体2=急诊分诊护士（五级分诊标准）。

> **【第2段：并行执行+综合融合】** 并行执行机制。综合分析节点如何融合双方意见。展示Prompt差异。

### 4.2.4 风险评估节点——Softmax概率计算

> **【第1段：匹配流程】** 症状匹配 → 风险因子评分 → Softmax公式 → 诊断检查推荐。

> **【第2段：Softmax公式】** 展示LaTeX公式：$P(i) = \frac{e^{s_i}}{\sum_{j=1}^{n} e^{s_j}}$，解释含义。展示计算代码片段。

### 4.2.5 多专家会诊节点

> **【第1段：三专家角色差异】** 诊断专家（病史+检查→诊断意见）、治疗专家（治疗方案）、影像专家（影像分析）。

> **【第2段：RAG+综合报告】** 每个专家先RAG检索知识支撑。三个专家Prompt设计差异。综合报告生成策略。

### 4.2.6 知识查询节点

> **【1段】** create_react_agent配置。ReAct推理循环：Thought→Action→Observation。展示一次完整的工具调用过程示例。

## 4.3 MCP工具服务实现

> **【第1段：框架+装饰器】** FastMCP框架，@mcp.tool()装饰器声明工具。SSE传输，端口8002。

> **【第2段：4个工具逐一说明】** 每个工具的实现逻辑和返回数据结构：
> 1. symptom_search_analyze — 向量相似度症状检索（HNSW）
> 2. get_common_diagnostic_methods — 通用诊断方法
> 3. get_diagnostic_tests_for_disease — 特定疾病诊断检查
> 4. retrieve_medical_knowledge_vector — Redis向量检索（FLAT）

> **【第3段：代码展示】** 截取MCP/mcp_server.py中一个工具的实现代码（≤15行）。

## 4.4 服务端知识管理实现

### 4.4.1 知识管理REST API

> **【1段】** 核心接口概述：upload/extract/build/list/load/delete/stats。RESTful规范+JSON格式。展示一个关键接口的代码片段。

### 4.4.2 异步处理与SSE进度推送

> **【第1段：异步方案】** 为什么异步——避免阻塞HTTP。流程：上传返回task_id → asyncio协程 → PDFProgressTracker。

> **【第2段：SSE推送】** EventSource订阅。展示PDFProgressTracker关键代码。可配时序图（图表.md中有）。

### 4.4.3 数据一致性管理

> **【1段】** 三方存储同步挑战。KnowledgeDataManager统一管理。sync-metadata + cleanup-orphaned接口。展示关键代码。

## 4.5 前端界面实现

### 4.5.1 客户端诊断界面

> **【1段】** Vue3三栏布局：PatientList | PatientDetail | ChatPanel。SSE流式对话实现。**主要靠截图展示**，每个截图配2-3句说明。

### 4.5.2 服务端知识管理界面

> **【1段】** Vue3开发。文献上传+进度（对接SSE）、知识图谱可视化（ECharts力导向图）、实体关系管理。**主要靠截图展示**。

## 4.6 系统部署实现

### LLM模型部署（重点写）

> **【第1段：部署链路】** AutoDL云GPU → Ollama部署Qwen2.5-14B → cpolar穿透固定域名。

> **【第2段：启动脚本】** 贴start.sh关键部分（Ollama服务启动+模型加载+cpolar配置）。

### 其他部署

> **【第1段：Docker容器化】** docker-compose编排4个容器（客户端/服务端/Neo4j/Redis）。

> **【第2段：混合部署方案】** 客户端本地（无GPU）+ 服务端云端 + AutoDL模型服务。客户端PyInstaller打包为可执行文件。

## 4.7 本章小结

> **【1段】** 串联：KG自动构建流水线 → 五节点多智能体工作流 → MCP标准化工具 → 服务端知识管理 → 前端界面 → 部署方案。
