# 第四章 系统详细设计与实现 — 写作思路

> 约 5000 字，这是论文最核心的章节，多展示实际做了什么（代码+截图+运行效果）。
> 代码片段每处不超过 15 行，建议以截图形式放入（不算查重）。

---

## 4.1 知识图谱自动化构建实现（约 4 页）

**4.1.1 PDF 文献解析（Docling）**
- 介绍 Docling 工作原理
- 展示关键代码：DocumentConverter 的调用
- 说明输出的 HTML 结构

**4.1.2 文本清洗与分块**
- 清洗策略：去除 DOI 之前内容、去除参考文献之后内容、删除多余空格
- 分块参数：chunk_size=2000, chunk_overlap=200
- 使用 RecursiveCharacterTextSplitter 的原因

**4.1.3 基于 LLM 的实体关系抽取**
- 实体类型和关系类型定义（Pydantic 模型）
- Prompt 设计：给 LLM 什么样的指令来抽取实体和关系
- 展示一个抽取结果的 JSON 示例

**4.1.4 Neo4j 图谱存储**
- py2neo 操作 Neo4j
- 节点和关系的创建代码
- 去重和合并策略（MERGE）

**4.1.5 症状向量化索引**
- M3E-Base 生成 768 维向量
- Neo4j 向量索引创建
- 向量相似度检索实现

**需要查找的文献：**
- Docling 技术文档（IBM 开源）、M3E 句向量模型说明

---

## 4.2 多智能体工作流实现（约 5 页）

**4.2.1 LangGraph 状态图构建**
- State 类定义（TypedDict）
- StateGraph 构建：add_node → add_edge → add_conditional_edges → compile
- InMemorySaver 实现多轮对话记忆

**4.2.2 监督节点——任务路由**
- Prompt 设计：如何让 LLM 区分"症状描述""病史回答""检查结果""知识查询"
- 上下文感知路由：has_triaged 和 has_diagnosis 状态判断

**4.2.3 分诊节点——并行分诊**
- 两个智能体角色设计
- 并行执行机制
- 综合分析融合策略 + 五级分诊标准

**4.2.4 风险评估节点——Softmax 概率计算**
- 症状匹配 → 风险因子评分 → Softmax 公式 → 诊断检查推荐
- 展示 Softmax 公式（LaTeX）：$P(i) = \frac{e^{s_i}}{\sum_{j=1}^{n} e^{s_j}}$

**4.2.5 多专家会诊节点**
- RAG 检索为每个专家提供知识支撑
- 三个专家 Prompt 设计差异
- 综合报告生成策略

**4.2.6 知识查询节点**
- create_react_agent 创建和配置
- ReAct 推理循环：Thought → Action → Observation

**需要查找的文献：**
- LangGraph 官方文档、ReAct 论文 (Yao et al., 2022)

---

## 4.3 MCP 工具服务实现（约 2 页）

- FastMCP 框架使用（@mcp.tool() 装饰器）
- 每个工具的实现逻辑和返回数据结构
- SSE 传输协议配置

---

## 4.4 服务端知识管理实现（约 2 页）

**4.4.1 知识管理 REST API**
- 核心接口表格：upload / extract / build / list / load / delete / stats
- 展示一个关键接口代码片段

**4.4.2 异步处理与 SSE 进度推送**
- 异步后台任务 + SSE 实时推送进度
- 流程：上传返回 task_id → asyncio 协程 → PDFProgressTracker → EventSource 订阅
- 展示 PDFProgressTracker 关键代码

**4.4.3 数据一致性管理**
- 三方存储（Neo4j + Redis + 文件系统）同步挑战
- KnowledgeDataManager 统一管理
- sync-metadata + cleanup-orphaned 接口

---

## 4.5 前端界面实现（约 2 页）

**4.5.1 客户端诊断界面**
- 三栏布局：PatientList | PatientDetail | ChatPanel
- SSE 流式对话实现

**4.5.2 服务端知识管理界面**
- 文献上传与处理进度（对接 SSE）
- 知识图谱可视化（ECharts）
- 实体关系管理

> ⚠️ 这两节主要靠截图展示，每个截图配 2-3 句文字说明即可

---

## 4.6 系统部署实现（约 1.5 页）

**LLM 模型部署（重点写）：**
- AutoDL 云 GPU 租用 → Ollama 部署 Qwen2.5-14B → cpolar 穿透固定域名
- 贴 start.sh 关键部分

**其他部署：**
- Docker 容器化（docker-compose）
- 混合部署方案：客户端本地 + 服务端云端 + AutoDL 模型服务
- 客户端 PyInstaller/Electron 打包

---

## 4.7 本章小结（0.5 页）

一段话总结本章各模块的实现情况。
